[packages]
node = "22"
pnpm = "10"

[phases.install]
cmds = [
  "pnpm add -g node-gyp",
  "pnpm install --no-frozen-lockfile --prefer-offline"
]

[phases.build]
cmds = [
  "cd artifacts/api-server && pnpm run build"
]

[start]
cmd = "cd artifacts/api-server && pnpm run start:cloud"
