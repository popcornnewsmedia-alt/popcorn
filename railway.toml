[build]
buildCommand = "pnpm install --no-frozen-lockfile && cd artifacts/api-server && pnpm run build"

[deploy]
startCommand = "cd artifacts/api-server && node --enable-source-maps ./dist/index.mjs"
restartPolicyType = "always"
